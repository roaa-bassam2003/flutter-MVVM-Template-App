import 'package:flutter/material.dart';

class ProviderMainView extends StatelessWidget {
  const ProviderMainView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Service Provider'),
    );
  }
}