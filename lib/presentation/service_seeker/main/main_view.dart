import 'package:flutter/material.dart';

class SeekerMainView extends StatelessWidget {
  const SeekerMainView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Service Seeker'),
    );
  }
}